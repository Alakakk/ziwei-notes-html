export function SiteFooter() {
  return (
    <footer className="site-footer">
      <p>内容为学习整理，不作现实决策依据。</p>
      <p>Private repo now, public site later only after explicit publishing setup.</p>
    </footer>
  );
}
